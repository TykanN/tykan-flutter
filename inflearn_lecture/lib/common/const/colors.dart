import 'package:flutter/material.dart';

abstract class MyColor {
  // 주색상
  static const primary = Color(0xff22a45d);
  // 글자 색상
  static const bodyText = Color(0xff868686);
  // 주색상
  static const inputBg = Color(0xfffbfbfb);
  // 글자 색상
  static const inputBroder = Color(0xfff3f2f2);
}
